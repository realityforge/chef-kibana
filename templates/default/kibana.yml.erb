# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
# !! Managed by Chef, do not edit !!
# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

<%
  @config.keys.map { |k| k.to_s }.sort.each do |key|
    # skip this config value if we've already seen it
    next if key.nil? || @config[key].nil? %>
<%= key %>: <%= @config[key] %>
<% end %>
